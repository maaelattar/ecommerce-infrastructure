# Use pnpm for package management
package-manager=pnpm

# Auto install peer dependencies
auto-install-peers=true

# Hoist dependencies for better performance
hoist-pattern[]=*

# Use exact versions for reproducible builds
save-exact=true